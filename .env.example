# Environment Configuration
# Copy this file to .env and update values

# Application Environment
# Options: development, production
NODE_ENV=development

# Server Configuration
CONTROL_SERVER_PORT=3000
CONTROL_SERVER_URL=http://localhost:3000

# SMS/Email Gateway Authentication (optional)
GATEWAY_API_KEY=

# Logging Configuration
# In production mode, only errors, warnings, and critical info are logged
# In development mode, all debug logs are shown
